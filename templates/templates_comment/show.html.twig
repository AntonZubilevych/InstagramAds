{% extends 'base.html.twig' %}

{% block title %}TemplatesComment{% endblock %}

{% block body %}
    <h1>TemplatesComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ templates_comment.id }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ templates_comment.author }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ templates_comment.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('templates_comment_index') }}">back to list</a>

    <a href="{{ path('templates_comment_edit', {'id': templates_comment.id}) }}">edit</a>

    {{ include('templates_comment/_delete_form.html.twig') }}
{% endblock %}
