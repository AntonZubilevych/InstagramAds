{% extends 'base.html.twig' %}

{% block title %}TemplatesDescription index{% endblock %}

{% block body %}
    <h1>TemplatesDescription index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Text</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for templates_description in templates_descriptions %}
            <tr>
                <td>{{ templates_description.id }}</td>
                <td>{{ templates_description.title }}</td>
                <td>{{ templates_description.text }}</td>
                <td>
                    <a href="{{ path('templates_description_show', {'id': templates_description.id}) }}">show</a>
                    <a href="{{ path('templates_description_edit', {'id': templates_description.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('templates_description_new') }}">Create new</a>
{% endblock %}
